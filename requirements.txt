# -------------------------------
# Core framework
# -------------------------------
fastapi==0.116.1
uvicorn[standard]==0.30.1

# -------------------------------
# LangChain ecosystem
# -------------------------------
langchain==0.3.0
langchain-elasticsearch==0.3.0
langchain-community==0.3.0
langchain-huggingface==0.1.0

# -------------------------------
# Google Drive ingestion
# -------------------------------
google-api-python-client==2.181.0
google-auth==2.31.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# -------------------------------
# PDF Processing (Modern AI-Powered Pipeline)
# -------------------------------
pypdf==4.2.0
pdf2image==1.17.0
pillow==10.3.0
pdfminer.six==20221105   # better text extraction fallback (compatible with pdfplumber)

# Modern PDF extraction libraries (superior to old Poppler + Tesseract)
PyMuPDF==1.23.14         # Best for text, metadata, images from normal PDFs
pdfplumber==0.10.3       # Excellent for complex layouts and tables

# Modern OCR alternatives (better than Tesseract)
PaddleOCR==2.7.3         # Superior OCR for scanned images and handwriting
EasyOCR==1.7.0           # Alternative OCR with better accuracy

# Layout analysis for complex documents
unstructured[pdf]==0.11.8  # Docling alternative for tables, multi-columns, layouts

# Image processing for OCR
opencv-python<=4.6.0.66  # Image preprocessing (compatible with PaddleOCR)
numpy==1.26.4            # Required by OpenCV/EasyOCR pipelines

# -------------------------------
# Elasticsearch
# -------------------------------
elasticsearch==8.15.0

# -------------------------------
# Embeddings
# -------------------------------
sentence-transformers==5.1.0

# -------------------------------
# Hugging Face
# -------------------------------
transformers==4.42.4
accelerate==0.33.0
torch>=2.2.0
safetensors>=0.4.3

# -------------------------------
# Web UI
# -------------------------------
streamlit==1.49.1
requests==2.32.3
python-dotenv==1.0.1

# -------------------------------
# Tokenization
# -------------------------------
tiktoken==0.11.0

# -------------------------------
# Data validation
# -------------------------------
pydantic==2.11.7

# -------------------------------
# Testing
# -------------------------------
pytest==8.4.1
httpx==0.27.2